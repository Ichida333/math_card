\begin{proof} 1. \\
  $f$が単射であると仮定するので以下は真.\\
  \begin{equation}
    \forall x_1, x_2 \in X (f(x_1) = f(x_2) \rightarrow x_1 = x_2)
  \end{equation}
  1.1を示す.\\
  $Y$が可算集合であると仮定するので以下は真.\\
  \begin{equation}
    \exists n \in \nat\exists f(\BijMap{f}{n}{Y}) \lor \exists f (\BijMap{f}{\nat}{Y})
  \end{equation}
\end{proof}
