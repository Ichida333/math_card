\setcounter{section}{0}
\section{自然数と加算集合}
\label{section:自然数と加算集合}

\begin{definition}[自然数を集合論的に構成, p16]
	\label{definition:集合論的な自然数の定義}
	以下のように,自然数$n$を集合論的に構成できる.\\
	$n = 0$のとき$n = \emptyset,n = 1 のときn = \mathcal{P}(\emptyset),
	n = 2のときn = \mathcal{P}(\mathcal{P}(\emptyset))と定義する.$\\
	このとき,$1=\mathcal{P}(\emptyset) = \{\emptyset\} = \emptyset \cup \{\emptyset\} = 0 \cup \{0\} = \{0\}.$\\
	また,$2=\mathcal{P}(\mathcal{P}(\emptyset))=\mathcal{P}(\{\emptyset\}) 
	= \{\emptyset, \{\emptyset\}\}  = 1 \cup \{1\} = \{0, 1\}$\\
	$n >= 3$のとき,$n = n - 1 \cup \{n - 1\}$と定義する.すると,$n = \{0,1,2,... n-1\}と表せる.$
\end{definition}

\begin{definition}[有限集合,無限集合の定義, p183]
	\label{definition:有限集合,無限集合の定義}
	$X$を集合とする。$X$が有限集合,あるいは無限集合であることを以下のように定義する.\\
	$ Xは有限集合 \defarr \exists n \in \mathbb{N}\exists f( \; \BijectionMap{f}{n}{X} \; )$\\
	$ Xは無限集合\defarr \forall n \in \mathbb{N}\forall f( \lnot \; \BijectionMap{f}{n}{X} \; )$\\
\end{definition}

\begin{proposition}[有限集合の元の数が確定する, p184]
	$n,m$を自然数とし,$f:n \rightarrow m$を全単射とする.このとき,$n=m$である.論理式は以下.\\
	$\forall n ,m \in \mathbb{N} (\exists f(\BijectionMap{f}{n}{m}) \rightarrow n = m)$
\end{proposition}

\begin{proof} $\varphi (n) :\Leftrightarrow \forall m \in \mathbb{N} (\exists f(\BijectionMap{f}{n}{m}) \rightarrow n = m)$とし、$n$に関する数学的帰納法で示す.\\

(goal) $\varphi (0) \land \forall n \in \mathbb{N} ( \varphi (n-1) \rightarrow \varphi (n))$\\

(base step)(goalA) $\varphi (0) \Leftrightarrow  \forall m \in \mathbb{N}  (\exists f(\BijectionMap{f}{0}{m}) \rightarrow 0 = m)$ \\
$m \neq 0$とし,背理法で示す.\\
任意に$m \in \mathbb{N}$をとる.また,$\exists f(\BijectionMap{f}{0}{m})$は真より,そんな$f$を一つとり固定.\\
$ 0 = \emptyset$より,$\BijectionMap{f}{\emptyset}{m}$は真.よって,$\SurjectionMap{f}{\emptyset}{m}$も真なので,以下が成り立つ.\\
$\forall y \in m \exists x \in \emptyset (f(x) = y)$\\
今、$m \neq \emptyset$なので,$\exists x \in \emptyset (f(x) = y)$が真であるが,
これは,$\forall x \in \emptyset$が偽であることに矛盾する.よって,goalAは真.\\

(induction step)(goalB) $\forall n \in \mathbb{N} ( \varphi (n-1) \rightarrow \varphi (n)) \\
\Leftrightarrow \forall n \in \mathbb{N}(\forall m' \in \mathbb{N}  (\exists f'(\BijectionMap{f'}{n-1}{m'}) \rightarrow  n-1 = m') \rightarrow 
\forall m \in \mathbb{N}  (\exists f(\BijectionMap{f}{n}{m}) \rightarrow  n = m))$ \\
任意に$n \in \mathbb{N}(n>0)$をとり,前件を仮定して,後件を示す.\\
任意に$m \in \mathbb{N}$をとる.このとき,$m \neq 0$である.\\
また,$\exists f(\BijectionMap{f}{n}{m})$は真より,そんな$f$を一つとり固定.\\
$l:=f(m-1)$とおく.\\
$g:=\{<k,k> \mid k \neq l, m-1 \} \cup \{<k,l> \mid k = m-1 \} \cup \{<k,m-1> \mid k = l \}$とする.\\
$g \circ g = id_m$なので,$\BijectionMap{g \circ g}{m}{m}$は真.よって,$\BijectionMap{g}{m}{m}$も真.\\
また,$\BijectionMap{f}{n}{m}, \BijectionMap{g}{m}{m}$より,$\BijectionMap{g \circ f}{n}{m}$も真.\\
$f':= g \circ f \setminus \{<n-1, m-1>\}$とおく.\\
$g \circ f(n-1) = g(l) = m-1$より,$\BijectionMap{f'}{n-1}{m-1}$は真.\\
$m-1 \in \mathbb{N}$と,帰納法の仮定より,$ \exists f'(\BijectionMap{f'}{n-1}{m-1})\rightarrow  n-1 = m-1$が真.\\
また、$f'$の存在より,$\exists f'(\BijectionMap{f'}{n-1}{m-1})$は真.ゆえに,$n-1 = m-1$は真.\\
$n=n-1 \cup \{n-1\}, m=m-1 \cup \{m-1\}$と表せる.\\
これと,$n-1=m-1$より,$n=m$.よってgoalBは真.\\
goalA $\land$ goalBが真より、goalは真.\\
\end{proof}

\begin{proposition}[自然数の集合$\mathbb{N}$は無限集合, p184]
  $\forall n \in \mathbb{N}\forall f( \lnot \; \BijectionMap{f}{n}{\mathbb{N}} \; )$
\end{proposition}


\begin{proof} $\varphi (n) :\Leftrightarrow \forall f( \lnot \; \BijectionMap{f}{n}{\mathbb{N}} \; )$とし、$n$に関する数学的帰納法で示す.\\
(goal) $\varphi (0) \land \forall n \in \mathbb{N} ( \varphi (n-1) \rightarrow \varphi (n))$\\

(base step)(goalA) $\varphi (0) \Leftrightarrow  \forall f( \lnot \; \BijectionMap{f}{0}{\mathbb{N}} \; )$ \\
背理法で示す.よって、$\exists f(  \; \BijectionMap{f}{0}{\mathbb{N}} \; )$が真とする.そんな$f$を一つとり固定する.\\
$ 0 = \emptyset$より,$\BijectionMap{f}{\emptyset}{\mathbb{N}}$は真.よって,$\SurjectionMap{f}{\emptyset}{\mathbb{N}}$も真なので,以下が成り立つ.\\
$\forall y \in \mathbb{N} \exists x \in \emptyset (f(x) = y)$\\
今、$\mathbb{N} \neq \emptyset$なので,$\exists x \in \emptyset (f(x) = y)$が真であるが,
これは,$\forall x \in \emptyset$が偽であることに矛盾する.よって,goalAは真.\\

(induction step)(goalB) $\forall n \in \mathbb{N}(n > 0) ( \varphi (n-1) \rightarrow \varphi (n)) 
\Leftrightarrow \forall n \in \mathbb{N}(n > 0) ( \forall f( \lnot \; \BijectionMap{f}{n - 1}{\mathbb{N}} \; ) \rightarrow \forall f( \lnot \; \BijectionMap{f}{n}{\mathbb{N}} \; ))$
任意に$n \in \mathbb{N}(n > 0)$をとる.
$\exists f(\; \BijectionMap{f}{n}{\mathbb{N}} \; )$を真とし,背理法で示す.\\
$\exists f(\; \BijectionMap{f}{n}{\mathbb{N}} \; )$が真より,そんな$f$を一つとり,固定.\\
$m := f(n-1)$とおく.\\
$g:=\{<k,k> \mid k<= m-1 \} \cup \{<k,k-1> \mid m < k \}$とおくと,$\map{f}{\{m\}}{\mathbb{N}}$である.\\
$f':=f \setminus \{<n-1, m>\}$とおく.すると,$\BijectionMap{f'}{n-1}{\mathbb{N} \setminus \{m\}}$\\
よって、$\BijectionMap{g \circ f'}{n-1}{\mathbb{N} \setminus \{m\}}$\\
これは,帰納法の仮定に矛盾する.よってgoalは真.
\end{proof}

\begin{definition}[]
	\label{definition:合論的な自然数の定義}
	$Xを集合とする.$\\
	1. $\forall n \in \mathbb{N} (\exists f (\BijectionMap{f}{n}{\mathbb{N}})) \rightarrow nをXの濃度と呼び,n=CardXと表す.$\\
	2. $\exists f (\BijectionMap{f}{\mathbb{N}}{X}) \rightarrow Xは加算無限集合であるといい,CardX=\aleph_0と表す.$\\
	$Xが有限集合であるか,無限集合であるとき,Xは加算であるという.$
\end{definition}
